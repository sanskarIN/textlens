# TextLens 2.0.12 — Source Milestone Notes

Date: 2026-08-19

Status: **source milestone prepared; stable cross-platform binary release evidence is not yet complete.**

## Version identity

The application version is synchronized at **2.0.12** in:

- `package.json`
- `src-tauri/Cargo.toml`
- `src-tauri/tauri.conf.json`

The packaged About UI resolves the version from Tauri runtime metadata.

Report schema remains **v2**. Application version 2.0.12 does not alter the report schema. See `docs/report-schema.md`.

## Source-complete highlights

- privacy-first local Rust/Tauri text analysis;
- Unicode-aware word/vocabulary/core metrics;
- keywords, bigrams, trigrams, and local keyword exclusions;
- whitespace and line-ending diagnostics;
- local file analysis with conservative encoding handling and compatible streaming mode;
- canonical JSON reports and customizable privacy-safe Markdown reports;
- validated report import and local report comparison;
- versioned settings backup/restore;
- opt-in path-free recent-file metadata;
- reusable local analysis presets;
- Quick actions and keyboard shortcuts;
- guarded startup and failure-safe local preference storage;
- manual, explicit GitHub Releases update navigation with no background polling;
- release version/tag consistency guards;
- deterministic SHA-256 artifact checksum generator/verifier;
- report schema-v2 compatibility freeze and regression guard;
- comprehensive repository/open-source documentation.

## Compatibility

### Analysis report JSON

- Emits: schema v2.
- Imports: valid schema v1 and schema v2.
- Rejects: version 0, unsupported future versions, malformed/oversized/inconsistent report data.
- Legacy v1 vocabulary fields absent from the old format are not shown as meaningful measured deltas.
- Raw analyzed text and full source paths remain outside the canonical report contract.

### Settings backup

- Current backup schema: v2.
- Compatible legacy schema-v1 restore remains supported with documented defaults for newer preferences.

## Privacy/security boundaries retained

- no account required;
- no analytics SDK;
- no cloud text-processing API;
- no background update polling;
- document contents processed locally;
- report exports omit raw source text;
- recent history is opt-in and path-free;
- storage failure does not trigger a network fallback;
- missing destination errors do not echo private directory paths;
- release checksum manifest verification rejects unsafe/traversing paths and digest mismatches.

## Verification performed for this version preparation

- repository state and roadmap re-audited after the final reliability merge;
- no open repository issues found at the start of this pass;
- no open pull requests found at the start of this pass;
- repository search found no unresolved `TODO`/`FIXME`/`HACK`/`XXX` implementation markers;
- application metadata synchronized to 2.0.12;
- the repository version-sync algorithm was exercised against 2.0.12 metadata and returned success;
- report-schema v2 contract documented and protected by a dedicated Rust regression assertion.

## External release gates not claimed as complete

The following require actual package-manager/native-platform execution and must remain unchecked until evidence exists:

- generate/review npm `package-lock.json` using npm;
- generate/review `src-tauri/Cargo.lock` using Cargo;
- complete clean frontend and Rust quality suites from committed lockfiles;
- record the release benchmark on a documented machine/toolchain;
- build packages from clean Windows, macOS, and Linux checkouts;
- install and manually exercise each produced package;
- native screen-reader, scaling, keyboard, and reduced-motion acceptance;
- real screenshots from verified release candidates;
- Windows signing and Apple signing/notarization when credentials are available;
- repository-admin branch protection/rules application;
- SHA-256 manifest generation/verification for the actual final release artifacts.

Do not publish a stable `v2.0.12` release merely because the source version was prepared. Complete and record the applicable release gates first.

---

**Made by the Sanskar**
