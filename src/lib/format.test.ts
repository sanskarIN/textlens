import { describe, expect, it } from "vitest";
import { clampInteger, formatBytes, formatDuration, formatInteger } from "./format";
describe("format helpers", () => { it("formats byte quantities", () => { expect(formatBytes(12)).toBe("12 B"); expect(formatBytes(1024)).toBe("1.00 KB"); expect(formatBytes(10 * 1024)).toBe("10.0 KB"); }); it("formats reading durations", () => { expect(formatDuration(0)).toBe("0 min"); expect(formatDuration(59)).toBe("< 1 min"); expect(formatDuration(60)).toBe("1 min"); expect(formatDuration(3660)).toBe("1 hr 1 min"); }); it("clamps integer settings", () => { expect(clampInteger(5.8, 1, 5)).toBe(5); expect(clampInteger(-2, 1, 5)).toBe(1); expect(clampInteger(Number.NaN, 1, 5)).toBe(1); }); it("formats integers for the active locale", () => { expect(formatInteger(42)).toContain("42"); }); });
