{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Minimal Android and iOS permissions for the TextLens mobile shell.",
  "windows": ["main"],
  "platforms": ["iOS", "android"],
  "permissions": ["core:default", "dialog:default", "opener:default"]
}
